/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options go here.
  // For now, we can leave it empty as we don't have any custom settings.
  // Example:
  // images: {
  //   remotePatterns: [
  //     {
  //       protocol: 'https',
  //       hostname: 'placehold.co',
  //     },
  //   ],
  // },
};

export default nextConfig;
